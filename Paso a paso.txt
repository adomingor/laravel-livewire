Extensiones instaladas

Livewire Intellisense Extension for VSCode (NOP, no me gustó)

Primeros pasos

                                    --------------------------------------------------------
                                        GITHUB COMO PUBLICAR A UN REPOSITORIO POR 1RA VEZ
                                    --------------------------------------------------------

1. Preparación en GitHub
    Crea el repositorio en GitHub.
    Copia la URL HTTPS del repositorio que te proporciona GitHub (https://github.com/adomingor/laravel-livewire.git).

2. Preparación en VS Code (Casa)
    Abre tu carpeta de proyecto en Visual Studio Code.Abre la terminal integrada (Terminal -> New Terminal o Ctrl + ñ o Ctrl + j).

3. Conectar local con remoto y subirEjecuta los siguientes comandos en la terminal de VS Code:
    Inicializar Git: Crea un repositorio local.
        git init

    Añadir archivos: Prepara todos los archivos para el commit.
        git add .

    Confirmar cambios (Commit): Guarda la instantánea de los archivos.
        git commit -m "Iniciando repositorio desde casa php artisan make:livewire createPost"

    Vincular repositorio remoto: Conecta tu proyecto local con el repositorio de GitHub (reemplaza la URL).
        git remote add origin https://github.com/adomingor/laravel-livewire.git

    Subir código (Push): Envía el código a GitHub.
        git push -u origin main

                                        --------------------------------------------------------
FIN                                        GITHUB COMO PUBLICAR A UN REPOSITORIO POR 1RA VEZ
                                        --------------------------------------------------------



cuando ejecute composer run dev no encontró vite, ejecutar
    npm install
--------------------------------------------------------
added 95 packages, and audited 96 packages in 15s

27 packages are looking for funding
  run `npm fund` for details

found 0 vulnerabilities
--------------------------------------------------------
Pasó de 80 a 150 MB

Listo a tirar magia! (Siguiendo el ejemplo de https://youtu.be/ai5yfLzBgVo)

php artisan make:livewire createPost


--------------------------------------------------------
cuando creo un componente con laravel livewire a los archivos le pone un rayo adelante del nombre ⚡create-post.blade.php por ejemplo, como evito eso

Ese rayo (⚡) es un emoji decorativo de Laravel Livewire para identificar componentes en el editor y se puede quitar desactivándolo en la configuración. Abre config/livewire.php y cambia 'render_on_load' => true, a false, o busca la opción class_namespace para personalizar el prefijo si usas una versión que lo permita

    No tengo livewire.php, hay que publicarlo

Publicar configuración: Si no tienes el archivo de configuración de Livewire, publícalo con:
php artisan vendor:publish --tag=livewire:config
Editar config/livewire.php: Busca la opción que controla los "stubs" (plantillas) o el prefijo de componente. En versiones modernas de Livewire, el rayo se puede eliminar modificando cómo Livewire genera los archivos.Alternativa - Personalizar Stubs: Puedes publicar los stubs (plantillas de archivos) de Livewire y quitar el rayo manualmente de ellos:bashphp artisan livewire:stubs
Luego, edita los archivos en la carpeta stubs/ que se acaba de crear en tu proyecto, eliminando el emoji ⚡ de los nombres de archivo.El emoji ⚡ es opcional y no afecta el funcionamiento de tu aplicación, solo sirve como indicador visual.
--------------------------------------------------------

            Retomando... dejo el nombre con el cochino rayo
    Cuando ejecuté php artisan make:livewire createPost creó un solo archivo donde está todo, Clase y vista (no es de mi agrado).
    
    Para que cree dos archivos como es la forma clásica...
php artisan make:livewire create-post --view

👉 Esto crea:

Clase → app/Livewire/CreatePost.php
Vista → resources/views/livewire/create-post.blade.php

            EN LIVEWIRE 4 YA NO FUNCIONA 👆 hay que separar a mano los archivos (vamos a usar uno solo archivo para seguir con la forma del framework)

SEGUIMOS ... Después de php artisan make:livewire createPost

Se crecó el archivo ⚡create-post.blade.php con clase y div vacíos
en el div hice un formulario con 2 entradas
    input type="text" id="title" wire:model="title" placeholder="Título del post" ....
    <textarea id="content" wire:model="content" rows="4" placeholder="Contenido del post" ...
Los wire:model= son para hacer referencia a las variables públicas de la clase (del mismo archivo en este caso)

En la clase cree las 2 variables
    public $title = '';
    public $content = '';

En el form está la línea 
    <form wire:submit.prevent="createPost" class="space-y-4">
wire:submit.prevent= es para hacer referencia a la función que se va a ejecutar en éste caso llama a createPost

-----------------------------------------------------------
        TENIA PROBLEMAS CON ALGUNAS FUNCIONES DEL wire:model (por ejemplo wire:model.blur o wire:model.change) consultando a Claudio...

En Livewire v3 (comportamiento antiguo):

Directiva	Comportamiento
wire:model.blur	Sincroniza con el servidor al perder el foco
wire:model.change	Sincroniza con el servidor al cambiar
En Livewire v4 (comportamiento nuevo - BREAKING CHANGE):

Directiva	Comportamiento
wire:model.blur	Solo actualiza el estado cliente (Alpine), NO envía al servidor
wire:model.change	Solo actualiza el estado cliente (Alpine), NO envía al servidor
wire:model.live.blur	Sincroniza con el servidor al perder el foco (= el viejo .blur)
wire:model.live.change	Sincroniza con el servidor al cambiar (= el viejo .change)
La lógica nueva en v4: los modificadores antes de .live controlan el timing del estado cliente (Alpine), los modificadores después de .live controlan cuándo se hace la petición al servidor.

    FENOMENAL EL CAMBIO!!! 
-----------------------------------------------------------

                        ----------------------------------------------------------------------------------------------------------------------
                                                    Ejemplo básico de contador con propiedad pública y privada
                        ----------------------------------------------------------------------------------------------------------------------
php artisan make:livewire contador (Lo mostramos en el dashboard)

                        ----------------------------------------------------------------------------------------------------------------------
                                                            Ejemplo básico de paso de parámetros
                        ----------------------------------------------------------------------------------------------------------------------
php artisan make:livewire nombre_x_parametro (Comenzamos en el dashboard)

                        ----------------------------------------------------------------------------------------------------------------------
                                                            Creando componentes
                        ----------------------------------------------------------------------------------------------------------------------
    Como se venía haciendo para crear un solo archivo 
php artisan make:livewire nombre_componente

    Para separar el controlador de la vista!!!! COOOOOOOOOL
php artisan make:livewire NombreClase --class (veamos si funca.. la primera letra con mayúsculas)

php artisan make:livewire ClaseSepradaDeVista --class

    Clase en app\Livewire\ClaseSepradaDeVista.php
    Vista en resources\views\livewire\clase-seprada-de-vista.blade.php
Funciona heeeeeeermoso!


                        ----------------------------------------------------------------------------------------------------------------------
                                                            Publicar archivo de configuración de livewire
                        ----------------------------------------------------------------------------------------------------------------------
php artisan livewire:config

    Crea el archivo config\livewire.php

    Cuando se crea un componente se puede pasarle parámetros sfc, mfc, class

    * SFC significa Single File Component (Componente de Archivo Único). En este formato, la lógica PHP (clase), la plantilla Blade (HTML) y, opcionalmente, los estilos CSS y scripts JavaScript, residen en un solo archivo con extensión .php o .blade.php, utilizando una sintaxis especial similar a los componentes de frameworks frontend como Vue.js.
    * MFC significa Multi-File Component (Componente de Múltiples Archivos). En este formato, la lógica PHP se encuentra en un archivo de clase (.php) separado de su respectiva plantilla Blade (.blade.php). Este era el enfoque convencional en versiones anteriores de Livewire (antes de v4).
    * Class se refiere específicamente al formato Class-based component (Componente basado en Clase). Es similar a MFC, pero se usa para referirse a la estructura tradicional donde el foco principal es la clase PHP que extiende de Livewire\Component y su vista asociada se encuentra en una ubicación separada, a menudo en resources/views/livewire


                        ----------------------------------------------------------------------------------------------------------------------
                                Creando un crud con livewire (luego veré con crud-generator  "composer require ibex/crud-generator --dev")
                        ----------------------------------------------------------------------------------------------------------------------
1 crear la migración de la tabla o el modelo si ya existe
    Creamos a mano el archivo Post.php

            use Illuminte\Database\Eloquuent\Model;

            class Post extends Model
            {
                public $timestamps = false;
                protected $fillable = ['title', 'content'];
            }
    Luego 
        php artisan make:livewire Post-crud
resources\views\components\post-crud.blade.php

UN DOLOR DE HUEVO HACERLO A MANO SIGIUENDO UN PUTO VIDEO


                        ----------------------------------------------------------------------------------------------------------------------
                                Creando un crud con crud-generator  "composer require ibex/crud-generator --dev"php artisan boost:install
                        ----------------------------------------------------------------------------------------------------------------------
composer require laravel/boost --dev
php artisan boost:install (cuando se estaba instalando preguntó que agente configurar, ahí estaba Claudito)
composer require ibex/crud-generator --dev

creemos la migración de la tabla (ya que no la tengo), le pedimos a Claudito que haga cree una migración 
    php artisan make:migration create_posts_table --no-interaction
Read d:\www\laravel\livewire-casa\database\migrations\2026_03_29_022024_create_posts_table.php
Edit d:\www\laravel\livewire-casa\database\migrations\2026_03_29_022024_create_posts_table.php
Para ejecutarla: php artisan migrate

Veamos ahora...

php artisan make:crud posts livewire  👈 hizo todo, pero usó los x-label en lugar de flux (ver como se tira el comando)

Usar con --flux 👇
    php artisan make:crud posts livewire --flux (tener instaldo Volt... me daba error cdo. quería generar Productos)


                        ----------------------------------------------------------------------------------------------------------------------
                                                                HERMOSO... AHORA VAMOS X UN CASO REAL
                        ----------------------------------------------------------------------------------------------------------------------
Trataré de hacer un CRUD, dejarlo con las opciones que más se usan, publicaré los stubs y con Claudio iremos dejándolo listo para que los próximos cruds ya tengan todo

    Traje las tablas de la otra bd y las puse en tablas_productos_categorias.txt, vamos a pedir a Claudio las migraciones para ya tenerlas en cualquier proyecto, luego hare el crud con crud-generator, y con Claudio iremos poniendo

🤖 Claude: crea las migarciones para las tablas que estan en tablas_productos_categorias.txt

Creamos el crud (Son cruds básicos que necesitan luego poner las relaciones a mano)
    php artisan make:crud Productos livewire --flux (ups, no estaban los modelos, da error en --flux, ahora veremos con el modelo)

🤖 Claude: crea el modelo para las tablas productos, categorias y prod_cat

El error con --flux es por que no está instalado Volt 
    composer require livewire/volt (ahora lo instalamos)
    Sigue dando error en --flux

    Para hacer interactiva la creación de cruds se pone solamente php artisan make:crud Productos
Solo aparecen esas opciones
     php artisan make:crud Productos

  Which stack would you like to install?
  Blade with Bootstrap css ............................................................................................................... bootstrap
  Blade with Tailwind css ................................................................................................................. tailwind
  Livewire with Tailwind css .............................................................................................................. livewire
  API only ..................................................................................................................................... api

desinstalo crud y vuelvo a instalarlo ya con volt instalado

    composer remove ibex/crud-generator
    php artisan config:clear
    php artisan cache:clear
    composer dump-autoload

Instalé de nuevo
    composer require ibex/crud-generator --dev
SIGUE DANDO ERROR EN --flux NO GENERA CON FLUX SEGÚN CHATGPT

    php artisan make:command MakeCrudPro

Crea app\Console\Commands\MakeCrudPro.php

php artisan make:crud-pro productos
php artisan optimize:clear

PERDIDA TOTAL DE TIEMPO CON CHAT-O

                RETOMEMOS CON crud-generator luego veremos los stubs (php artisan vendor:publish --tag=crud)
php artisan make:crud productos livewire (productos nombre de la tabla en postgresql)

Running Crud Generator ...
Creating Livewire Component ...

 Already exist Model. Do you want overwrite (y/n)? [y] (hice una copia y luego renombre)

 php artisan make:crud categorias livewire


                        ----------------------------------------------------------------------------------------------------------------------
                                                        INSTALAMOS LOS IDIOMAS, CLAUDE PARA COMANDO Y SEGUIMOS
                        ----------------------------------------------------------------------------------------------------------------------
composer require laravel-lang/common --dev
php artisan lang:add es

    Luego en el .env configurar
APP_LOCALE=es
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=es_AR

npm install -g @anthropic-ai/claude-code

                            ----------------------------------------------------------------------------------------------------------------------
FIN                                                      INSTALAMOS LOS IDIOMAS, CLAUDE PARA COMANDO Y SEGUIMOS
                            ----------------------------------------------------------------------------------------------------------------------



Realizando pruebas de crud-generator con livewire

php artisan make:crud pruebas livewire
Running Crud Generator ...
Creating Livewire Component ...
Creating Model ...
Creating Request Class ...
Creating Views ...
Please add route below: i:e; web.php or api.php

Route::get('/pruebas', \App\Livewire\Pruebas\Index::class)->name('pruebas.index');
Route::get('/pruebas/create', \App\Livewire\Pruebas\Create::class)->name('pruebas.create');
Route::get('/pruebas/show/{prueba}', \App\Livewire\Pruebas\Show::class)->name('pruebas.show');
Route::get('/pruebas/update/{prueba}', \App\Livewire\Pruebas\Edit::class)->name('pruebas.edit');

Created Successfully.

----------------------------------------------------------------------------------------------------------------------
    Para mostrar el nombre de usuario en lugar del id, en el index de productos (por ejemplo)

    <td class="whitespace-nowrap px-3 py-4 text-sm text-zinc-600 dark:text-zinc-400">
        {{ $producto->user?->name ?? 'N/A' }}
    </td>


----------------------------------------------------------------------------------------------------------------------
    Agrupar los botones en el index

    td class="whitespace-nowrap py-4 pl-4 pr-3 text-sm font-medium">
        <flux:dropdown>
            <!-- Botón trigger (ícono) -->
            <flux:button variant="ghost" size="sm" icon="ellipsis-horizontal" />

            <!-- Contenido del dropdown -->
            <flux:menu>
                <flux:menu.item href="{{ route('productos.show', $producto->id) }}"
                    wire:navigate icon="eye">
                    {{ __('Ver') }}
                </flux:menu.item>

                <flux:menu.item href="{{ route('productos.edit', $producto->id) }}"
                    wire:navigate icon="pencil">
                    {{ __('Editar') }}
                </flux:menu.item>

                <flux:menu.item wire:click="delete({{ $producto->id }})"
                    wire:confirm="¿Estás seguro de que quieres eliminar este producto?"
                    icon="trash" variant="danger">
                    {{ __('Eliminar') }}
                </flux:menu.item>
            </flux:menu>
        </flux:dropdown>
    </td>

----------------------------------------------------------------------------------------------------------------------

    El estándar de la industria para administrar roles y permisos en Laravel es el paquete spatie/laravel-permission.
        Características y Uso del Paquete Spatie:
            Instalación: Se instala fácilmente mediante composer require spatie/laravel-permission.
            Gestión: Permite crear roles (ej. 'admin', 'editor') y permisos (ej. 'editar artículos').
            Asignación: Se añaden permisos a roles ($role->givePermissionTo($permission)) y roles a usuarios ($user->assignRole('writer')).
            Verificación: Ofrece métodos sencillos en el modelo de usuario como $user->hasRole('admin') o $user->can('editar artículos').
            Middleware: Soporta middleware para proteger rutas directamente: Route::group(['middleware' => ['role:admin']], ...)

----------------------------------------------------------------------------------------------------------------------


                        ----------------------------------------------------------------------------------------------------------------------
                                                        ROLES Y PERMISOS - COMENZAMOS CON LA SEGURIDAD (Spatie Permission + Filament)
                        ----------------------------------------------------------------------------------------------------------------------
                                    🧠 IDEA GENERAL
                                    Spatie → maneja roles y permisos (BD + lógica)
                                    Filament → usa policies + permisos para mostrar/ocultar cosas
                                    Vos conectás ambos
                                    Resumen de la estrategia ideal:
                                    Instala Spatie Laravel Permission.
                                    Crea una pantalla de administración en tu web (puedes usar Filament para hacerlo en 5 minutos).
                                    En tus archivos Blade, usa siempre @can('nombre_del_permiso').
                                    A partir de ahí, cualquier cambio de "quién hace qué" se hace desde la web, sin tocar código.                                                        
                        ----------------------------------------------------------------------------------------------------------------------
    Se realizó respaldos de las bases de datos liveire y breeze para poder volver atrás

    🍺 Spatie Laravel Permission.

composer require spatie/laravel-permission
php artisan vendor:publish --provider="Spatie\Permission\PermissionServiceProvider"
php artisan optimize:clear
php artisan config:clear
php artisan migrate
----------------------------------------------------------------------------------------------------------------------
Agregar al modelo User.php

use Spatie\Permission\Traits\HasRoles; 💥<- Aqui HasRoles

#[Fillable(['name', 'email', 'password'])]
#[Hidden(['password', 'two_factor_secret', 'two_factor_recovery_codes', 'remember_token'])]
class User extends Authenticatable
{
    /** @use HasFactory<UserFactory> */
    use HasFactory, Notifiable, TwoFactorAuthenticatable, HasRoles; 💥<- Aqui HasRoles
----------------------------------------------------------------------------------------------------------------------
    🍺 Filament.
composer require filament/filament:"^5.0"  (Si se usa desde poweshell composer require filament/filament:"~5.0")
php artisan filament:install --panels
     What is the panel's ID? [admin]
    All done! Would you like to show some love by starring the Filament repo on GitHub? (yes/no) [yes]

php artisan vendor:publish --tag=filament-config

Ingresé normalmente y lleva al dashboard
Ahora si pongo localhost:8000/admin me lleva a la pantalla de "administración" 😲 (la ruta no está en el web.php, en realidad es una pantalla incial)
    Está en app/Providers/Filament/AdminPanelProvider.php
Modificamos el  ->path('admin') por ->path('dashboard') y lleva al menú original que tenía

    🧠 IDEA
    Filament → interfaz (pantalla admin)
    Spatie → guarda roles y permisos
    Blade → usa @can() (Laravel nativo)
    ✅ OPCIÓN RÁPIDA (RECOMENDADA)

🤖 Claude creó todo, le pasé Esto:
ya instalé 
Spatie Permissions v7.2.2
Filament v5.4.3
Livewire v4.2.2
ibex/crud-generator (Laravel CRUD Generator) v2.1.9


usando estos comandos
composer require spatie/laravel-permission
php artisan vendor:publish --provider="Spatie\Permission\PermissionServiceProvider"
php artisan optimize:clear
php artisan config:clear
php artisan migrate

composer require filament/filament:"^5.0"
php artisan filament:install --panels
php artisan vendor:publish --tag=filament-config

Con Livewire ya tenía un panel con un dashboard
Ahora con filament, tengo otro panel en la ruta /admin, básico

Ya tengo cruds generados de productos y uno de una tabla pruebas..

Como implemento los roles y permisos

🧠 Plan de implementación
1. Controlar acceso al panel /admin
Implementar FilamentUser en el User model para que solo usuarios con rol admin puedan entrar:


// User model implementa FilamentUser
public function canAccessPanel(Panel $panel): bool
{
    if ($panel->getId() === 'admin') {
        return $this->hasRole('admin');
    }
    return true;
}
2. Crear roles y permisos via Seeder
Definir los permisos (ej: ver productos, crear productos, editar productos, eliminar productos) y asignarlos a roles (admin, editor, viewer).

3. Resources de gestión en Filament
Crear Resources de Filament para gestionar Roles y Permisos desde /admin:

RoleResource — listar/crear/editar roles y asignarles permisos
Asignar roles a usuarios
4. Proteger los Resources existentes con Policies
Crear Policies para Producto, Prueba, etc. que usen hasPermissionTo() de Spatie. Filament las detecta automáticamente.

¿Por dónde quieres empezar? Te propongo este orden:

a) Crear el Seeder de roles/permisos + implementar canAccessPanel()

b) Crear los Resources de gestión (Roles, Users con roles)

c) Crear las Policies para los CRUDs existentes (Producto, Prueba)

¿Los permisos son granulares por modelo (ej: productos.crear, productos.editar) o más simples por rol (admin puede todo, editor puede crear/editar, viewer solo ver)?

            🙋‍♂️ hazlo en ese orden, cuando termines uno, muestrame como usarlo

🤖 Paso a) completado — Cómo usarlo
1. Asignar rol admin a un usuario existente

php artisan tinker --execute '
  $user = App\Models\User::where("email", "tu@email.com")->first();
  $user->assignRole("admin");
  echo "Rol asignado: " . $user->getRoleNames()->first();
'
O crear un usuario admin nuevo:

php artisan make:filament-user
# luego asignarle el rol con tinker

2. Lo que está activo ahora
Rol	Acceso /admin	Puede hacer
admin	✅	Todo
editor	❌	Ver/crear/editar productos, categorías, pruebas
viewer	❌	Solo ver productos, categorías, pruebas

Sin rol admin nadie puede entrar al panel /admin. En entorno local esto no aplica hasta que el APP_ENV no sea local — en producción es obligatorio.

3. Para volver a correr el seeder (si cambiás permisos)

php artisan db:seed --class=RolesAndPermissionsSeeder

🤖 Paso b) completado — Cómo usarlo
Abrí /admin con un usuario que tenga rol admin. En el sidebar vas a ver el grupo Administración con dos ítems:

Gestión de Roles (/admin/roles)
Listar roles con cantidad de permisos y usuarios asignados
Crear / Editar un rol: ponés el nombre y tildás los permisos con un checkbox list buscable y con "seleccionar todos"
Gestión de Usuarios (/admin/users)
Listar usuarios con sus roles como badges
Crear usuarios: nombre, email, contraseña + roles asignados
Editar usuarios: podés cambiar sus roles; la contraseña queda vacía y solo se actualiza si escribís una nueva
Asignar el rol admin al primer usuario (si todavía no lo hiciste)

php artisan tinker --execute '
  $user = App\Models\User::first();
  $user->assignRole("admin");
  echo $user->name . " → " . $user->getRoleNames()->implode(", ");
'

🤖 Paso c) completado — Cómo funciona ahora
Lo que Filament hace automáticamente con las Policies
Cuando un usuario entra al panel /admin, Filament consulta la policy de cada Resource antes de mostrar o permitir acciones:

Acción	Método de la Policy	Efecto en Filament
Ver ítem en sidebar	viewAny()	Oculta el ítem si false
Entrar al listado	viewAny()	403 si false
Botón "Crear"	create()	Oculta el botón
Botón "Editar"	update()	Oculta el botón por fila
Botón "Eliminar"	delete()	Oculta el botón por fila
Eliminar en masa	deleteAny()	Oculta la acción bulk
Resumen de permisos por rol
Recurso	admin	editor	viewer
Productos	Ver + Crear + Editar + Eliminar	Ver + Crear + Editar	Solo ver
Categorías	Ver + Crear + Editar + Eliminar	Ver + Crear + Editar	Solo ver
Pruebas	Ver + Crear + Editar + Eliminar	Ver + Crear + Editar	Solo ver
Usuarios	Ver + Crear + Editar + Eliminar	—	—
Roles	Ver + Crear + Editar + Eliminar	—	—
Para ajustar permisos de un rol desde /admin/roles
Desde el panel podés editar cualquier rol y tildar/destildar permisos en tiempo real — sin tocar código.
                        ----------------------------------------------------------------------------------------------------------------------
FIN                                                     COMENZAMOS CON LA SEGURIDAD (Spatie) ROLES Y PERMISOS
                        ----------------------------------------------------------------------------------------------------------------------

🙋‍♂️ Claude... modifica los stubs de crud-generator para que funcionen con spatie permission y filament
🤖 Todo completado — Cómo usarlo
Nuevo comando único 😲😲😲😲😲😲😲

php artisan make:crud-full posts
Genera en un solo paso:

Livewire CRUD — app/Livewire/Posts/{Index,Create,Edit,Show}.php + Forms/PostForm.php
Vistas Blade — resources/views/livewire/post/*.blade.php
Policy — app/Policies/PostPolicy.php con los 6 permisos Spatie
Filament Resource — app/Filament/Resources/Posts/{PostResource,Schemas/PostForm,Tables/PostsTable,Pages/*}.php
Al terminar te muestra los pasos restantes:


  1. Add to RolesAndPermissionsSeeder:
       'ver posts', 'crear posts', 'editar posts', 'eliminar posts'
  2. Run: php artisan db:seed --class=RolesAndPermissionsSeeder
  3. Add route group to routes/web.php: ...
Qué generan los stubs ahora
Archivo	Qué tiene de nuevo
livewire/index.stub	$this->authorize('delete', ...)
livewire/create.stub	$this->authorize('create', ...) en mount + save
livewire/edit.stub	$this->authorize('update', ...) en mount + save
livewire/show.stub	$this->authorize('view', ...)
model.stub	use HasFactory
views/livewire/index.stub	Botones Agregar/Editar/Eliminar/Ver con @can
policy.stub	Policy con hasPermissionTo() de Spatie
filament/Resource.stub	Filament Resource con grupo de navegación
filament/Form.stub + Table.stub	Campos generados automáticamente desde la tabla
filament/Pages/*.stub	Pages de List/Create/Edit
